---
// src/pages/index.astro - ROOFINGMETER VERSION
import { getCollection } from 'astro:content';
import Base from "@/layouts/Base.astro";
import Posts from "@/layouts/components/Posts.astro";
import Pagination from "@/layouts/components/Pagination.astro";
import { CATEGORY_HIERARCHY } from '@/lib/categories';

// Helper function for automatic readTime calculation
function calculateReadTime(content, wordsPerMinute = 130) {
  if (!content) return 10;
  
  const plainText = content
    .replace(/---[\s\S]*?---/, '')
    .replace(/```[\s\S]*?```/g, '')
    .replace(/<[^>]*>/g, '')
    .replace(/#{1,6}\s*/g, '')
    .replace(/[\[\]\(\)]/g, ' ')
    .replace(/\s+/g, ' ')
    .trim();
  
  const wordCount = plainText.split(/\s+/).length;
  const minutes = Math.ceil(wordCount / wordsPerMinute);
  
  return Math.max(1, Math.min(minutes, 60));
}

function getPostReadTime(post) {
  if (post.data.readTime) return post.data.readTime;
  if (post.body) return calculateReadTime(post.body);
  
  const estimates = {
    'roof-cost-estimates': 8,
    'material-comparisons': 10,
    'contractor-guides': 12,
    'repair-vs-replace': 15,
    'maintenance-tips': 12,
    'local-contractors': 14
  };
  
  return estimates[post.data.category] || 10;
}

const allPostsRaw = await getCollection('posts', ({ data }) => !data.draft);
const allPosts = allPostsRaw.map(post => ({
  ...post,
  data: {
    ...post.data,
    readTime: getPostReadTime(post)
  }
}));

const sortedPosts = allPosts.sort(
  (a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime()
);

const featuredPost = sortedPosts[0];
const latestPosts = sortedPosts.slice(1, 8);

const categoryPosts = {};
const categoryPostCounts = {};

Object.keys(CATEGORY_HIERARCHY).forEach(category => {
  const allPostsInCategory = sortedPosts.filter(p => p.data.category === category);
  categoryPostCounts[category] = allPostsInCategory.length;
  categoryPosts[category] = allPostsInCategory
    .filter(p => p.slug !== featuredPost?.slug)
    .slice(0, 3);
});

const roofingBasicsPosts = sortedPosts
  .filter(p => p.data.category === 'roof-cost-estimates' && p.slug !== featuredPost?.slug);

let quickGuides;
if (roofingBasicsPosts.length >= 4) {
  quickGuides = roofingBasicsPosts.slice(0, 4);
} else {
  const otherPosts = sortedPosts
    .filter(p => p.slug !== featuredPost?.slug)
    .slice(0, 4 - roofingBasicsPosts.length);
  quickGuides = [...roofingBasicsPosts, ...otherPosts].slice(0, 4);
}

if (!quickGuides || quickGuides.length === 0) {
  quickGuides = sortedPosts.slice(0, 4);
}

const realisticStats = {
  homeownersHelped: allPosts.length > 0 ? `${allPosts.length * 100}+` : "500+",
  contractorsListed: "45+",
  costCalculations: "1,200+",
  moneySaved: "$2.5M+"
};
---

<Base title="RoofingMeter - Accurate Roof Cost Estimates & Contractor Guides" 
      description="Get free roofing cost estimates, material comparisons, and find trusted local contractors. Make informed decisions for your roofing project.">
  
  <!-- 🏠 ROOFING HERO SECTION -->
  <section class="relative min-h-[85vh] overflow-hidden bg-gradient-to-br from-blue-50 via-white to-amber-50">
    <!-- Roof Pattern Background -->
    <div class="absolute inset-0 opacity-5">
      <div class="absolute inset-0" style="
        background-image: 
          linear-gradient(45deg, #1e3a8a 2px, transparent 2px),
          linear-gradient(-45deg, #1e3a8a 2px, transparent 2px);
        background-size: 60px 60px;
        background-position: 0 0, 30px 30px;
      "></div>
    </div>
    
    <!-- Roofing Tools Pattern -->
    <div class="absolute inset-0 opacity-10">
      <div class="absolute top-1/4 left-10 text-6xl">🔨</div>
      <div class="absolute top-1/3 right-20 text-5xl">🏠</div>
      <div class="absolute bottom-1/4 left-1/4 text-4xl">💰</div>
      <div class="absolute bottom-1/3 right-1/3 text-6xl">📏</div>
    </div>
    
    <!-- Gradient Overlays -->
    <div class="absolute inset-0 bg-gradient-to-r from-blue-500/5 via-transparent to-amber-500/5"></div>
    <div class="absolute bottom-0 left-0 right-0 h-32 bg-gradient-to-t from-white to-transparent"></div>
    
    <div class="container relative z-10 mx-auto px-4 pt-28 pb-16">
      <div class="max-w-5xl mx-auto text-center">
        
        {/* Trust Badge */}
        <div class="inline-flex items-center gap-2 bg-white/90 backdrop-blur-sm border border-blue-400/30 rounded-full px-5 py-2.5 mb-8">
          <span class="text-blue-600">🏠</span>
          <span class="text-blue-700 font-medium text-sm">
            TRUSTED BY HOMEOWNERS • FREE ESTIMATES • NO COMMISSIONS
          </span>
        </div>
        
        {/* Main Headline */}
        <h1 class="text-4xl md:text-6xl lg:text-7xl font-black text-center mb-8 leading-tight">
          <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-600 via-blue-500 to-amber-500">
            Stop Overpaying
          </span>
          <br />
          <span class="text-gray-900 text-3xl md:text-5xl lg:text-6xl">
            For Your Roofing Project
          </span>
        </h1>
        
        {/* Subheadline */}
        <p class="text-xl md:text-2xl text-gray-700 mb-10 max-w-3xl mx-auto">
          Get accurate cost estimates, compare materials, and find trusted contractors.
          <span class="text-blue-600 font-semibold"> Save thousands on your next roofing project.</span>
        </p>
        
        {/* CTA Buttons */}
        <div class="flex flex-col sm:flex-row gap-5 justify-center mb-12">
          <a href="#cost-calculator" 
             class="group relative px-8 py-4 bg-gradient-to-r from-blue-600 to-blue-700 text-white font-bold rounded-xl hover:shadow-2xl hover:shadow-blue-500/40 transition-all duration-300 transform hover:-translate-y-1 text-center text-lg">
            <span class="relative z-10">💰 GET FREE COST ESTIMATE</span>
            <div class="absolute inset-0 rounded-xl bg-gradient-to-r from-blue-600 to-blue-700 blur opacity-50 group-hover:opacity-70 transition-opacity"></div>
          </a>
          <a href="#find-contractors" 
             class="px-8 py-4 bg-white/80 backdrop-blur-sm border-2 border-blue-500/30 text-gray-900 font-bold rounded-xl hover:bg-blue-500/10 hover:border-blue-500 transition-all duration-300 text-center text-lg">
            👷 FIND LOCAL CONTRACTORS
          </a>
        </div>
        
        {/* Stats */}
        <div class="bg-white/70 backdrop-blur-sm rounded-2xl border border-gray-200 p-6 max-w-3xl mx-auto">
          <div class="grid grid-cols-2 md:grid-cols-4 gap-6">
            <div class="text-center">
              <div class="text-3xl font-bold text-gray-900 mb-2">{realisticStats.homeownersHelped}</div>
              <div class="text-gray-600 text-sm">HOMEOWNERS HELPED</div>
            </div>
            <div class="text-center">
              <div class="text-3xl font-bold text-gray-900 mb-2">{realisticStats.contractorsListed}</div>
              <div class="text-gray-600 text-sm">TRUSTED CONTRACTORS</div>
            </div>
            <div class="text-center">
              <div class="text-3xl font-bold text-gray-900 mb-2">{realisticStats.costCalculations}</div>
              <div class="text-gray-600 text-sm">COST CALCULATIONS</div>
            </div>
            <div class="text-center">
              <div class="text-3xl font-bold text-green-600 mb-2">{realisticStats.moneySaved}</div>
              <div class="text-green-600 text-sm font-semibold">ESTIMATED SAVINGS</div>
            </div>
          </div>
        </div>
        
        {/* Trust Note */}
        <p class="text-gray-600 text-sm mt-8 max-w-xl mx-auto">
          ⚠️ <span class="text-gray-700">No hidden fees. No contractor kickbacks. Just honest information.</span>
        </p>
      </div>
    </div>
  </section>

  <!-- ====================== -->
  <!-- AD SLOT 1: PREMIUM ABOVE THE FOLD -->
  <!-- ====================== -->
  <div class="container mx-auto px-4 py-8">
    <div class="bg-gradient-to-r from-blue-50 to-amber-50 border-2 border-dashed border-blue-300 rounded-2xl p-6 text-center">
      <div class="text-blue-600 font-bold mb-3">ADVERTISEMENT</div>
      <div class="text-gray-600 text-sm mb-4">Premium ad placement - Above the fold visibility</div>
      <div class="bg-white/80 p-4 rounded-xl border border-blue-200">
        <div class="h-[90px] flex flex-col items-center justify-center">
          <span class="text-gray-400 text-lg font-medium">728x90 Leaderboard Ad</span>
          <span class="text-gray-500 text-sm mt-2">High visibility for roofing products/services</span>
        </div>
      </div>
      <div class="mt-4 text-xs text-gray-500">
        Contact: advertising@roofingmeter.com • High-traffic position
      </div>
    </div>
  </div>

  <!-- 🏗️ 6 ROOFING PILLARS -->
  <section class="py-20 bg-white">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-6">
          <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-600 to-amber-500">
            Your Complete Roofing Toolkit
          </span>
        </h2>
        <p class="text-xl text-gray-600 max-w-3xl mx-auto">
          Everything you need to make smart decisions about your roof, from cost estimates to contractor selection.
        </p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-6xl mx-auto">
        {Object.entries(CATEGORY_HIERARCHY).map(([category, data]) => (
          <div class="group relative">
            <div class="absolute inset-0 bg-gradient-to-br from-blue-50 to-amber-50 rounded-2xl transform group-hover:scale-105 transition-transform duration-300"></div>
            <div class="relative bg-white border border-gray-200 rounded-2xl p-8 hover:border-blue-400 transition-all hover:-translate-y-2 shadow-lg hover:shadow-xl">
              <div class="flex items-start gap-4 mb-6">
                <div class="text-4xl p-3 bg-gradient-to-br from-blue-100 to-amber-100 rounded-xl">
                  {data.icon}
                </div>
                <div>
                  <h3 class="text-xl font-bold text-gray-900 mb-2">{data.name}</h3>
                  <p class="text-blue-600 font-semibold text-sm">
                    {categoryPostCounts[category] || 0} Guides
                  </p>
                </div>
              </div>
              
              <p class="text-gray-600 text-sm mb-8">
                {data.description}
              </p>
              
              <div class="space-y-3 mb-8">
                {Object.entries(data.subcategories).slice(0, 3).map(([subSlug, subName]) => (
                  <div class="flex items-center gap-3 p-3 bg-gray-50 rounded-lg group-hover:bg-blue-50 transition-colors">
                    <div class="w-2 h-2 bg-blue-500 rounded-full"></div>
                    <span class="text-sm text-gray-700">{subName}</span>
                  </div>
                ))}
              </div>
              
              <a href={`/categories/${category}`} 
                 class="inline-flex items-center gap-2 text-blue-600 font-semibold text-sm hover:text-blue-700 transition-colors">
                Explore {data.name.toLowerCase()}
                <svg class="w-4 h-4 transform group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" />
                </svg>
              </a>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ====================== -->
  <!-- AD SLOT 2: MID-PAGE BANNER -->
  <!-- ====================== -->
  <div class="container mx-auto px-4 py-8">
    <div class="bg-gradient-to-r from-blue-100 to-amber-100 border-2 border-blue-400/50 rounded-2xl p-6 text-center">
      <div class="flex items-center justify-between mb-4">
        <div class="text-blue-700 font-bold">SPONSORED CONTENT</div>
        <div class="text-xs text-gray-600 bg-white/80 px-3 py-1 rounded-full">High Engagement</div>
      </div>
      <div class="bg-white rounded-xl p-4 shadow-md">
        <div class="h-[250px] flex items-center justify-center">
          <div class="text-center">
            <div class="text-4xl mb-3">🏠</div>
            <div class="text-gray-700 font-medium text-lg">Roofing Material Brands</div>
            <div class="text-gray-500 text-sm mt-2">Perfect placement for roofing manufacturers</div>
            <div class="mt-4 text-gray-400">300x250 Medium Rectangle</div>
          </div>
        </div>
      </div>
      <div class="mt-4 text-xs text-gray-600">
        Positioned between content sections for maximum visibility
      </div>
    </div>
  </div>

  <!-- 💰 COST CALCULATOR PREVIEW -->
  <section id="cost-calculator" class="py-20 bg-gradient-to-br from-blue-50 to-amber-50">
    <div class="container mx-auto px-4">
      <div class="max-w-6xl mx-auto">
        <div class="bg-white rounded-3xl overflow-hidden shadow-2xl">
          <div class="grid grid-cols-1 lg:grid-cols-2">
            {/* Calculator Form */}
            <div class="p-8 lg:p-12">
              <div class="mb-8">
                <div class="inline-flex items-center gap-2 mb-4">
                  <div class="px-4 py-2 bg-gradient-to-r from-blue-600 to-blue-700 rounded-full text-white font-bold text-sm">
                    🧮 FREE CALCULATOR
                  </div>
                  <div class="text-green-600 text-sm font-semibold">
                    INSTANT ESTIMATE
                  </div>
                </div>
                
                <h2 class="text-3xl lg:text-4xl font-bold text-gray-900 mb-4">
                  Get Your <span class="text-blue-600">Free Roof Cost Estimate</span>
                </h2>
                
                <p class="text-lg text-gray-700 mb-8">
                  Answer a few questions for a personalized cost estimate
                </p>
              </div>
              
              <div class="space-y-6">
                <div>
                  <label class="block text-gray-700 font-medium mb-2">Roof Size (sq. ft.)</label>
                  <input type="range" min="1000" max="5000" value="2500" 
                         class="w-full h-2 bg-gray-200 rounded-lg appearance-none cursor-pointer">
                  <div class="flex justify-between text-sm text-gray-600 mt-2">
                    <span>1,000</span>
                    <span>2,500 sq. ft. (Average)</span>
                    <span>5,000+</span>
                  </div>
                </div>
                
                <div>
                  <label class="block text-gray-700 font-medium mb-2">Material Type</label>
                  <div class="grid grid-cols-2 gap-3">
                    <button class="p-4 bg-blue-50 border border-blue-200 rounded-xl text-center hover:bg-blue-100 transition-colors">
                      <div class="text-2xl mb-2">🏠</div>
                      <div class="font-medium text-gray-900">Asphalt Shingles</div>
                      <div class="text-sm text-gray-600">$8,000-$16,000</div>
                    </button>
                    <button class="p-4 bg-gray-50 border border-gray-200 rounded-xl text-center hover:bg-gray-100 transition-colors">
                      <div class="text-2xl mb-2">🔩</div>
                      <div class="font-medium text-gray-900">Metal Roofing</div>
                      <div class="text-sm text-gray-600">$16,000-$36,000</div>
                    </button>
                  </div>
                </div>
                
                <div>
                  <label class="block text-gray-700 font-medium mb-2">Your Location</label>
                  <select class="w-full p-4 bg-gray-50 border border-gray-200 rounded-xl">
                    <option>Select your state...</option>
                    <option>California</option>
                    <option>Texas</option>
                    <option>Florida</option>
                    <option>New York</option>
                    <option>Other</option>
                  </select>
                </div>
                
                <button class="w-full py-4 bg-gradient-to-r from-blue-600 to-blue-700 text-white font-bold rounded-xl hover:shadow-xl hover:shadow-blue-500/30 transition-all hover:scale-[1.02]">
                  🔍 CALCULATE MY COST ESTIMATE
                </button>
              </div>
            </div>
            
            {/* Results Preview */}
            <div class="bg-gradient-to-br from-blue-600 to-blue-800 p-8 lg:p-12 text-white">
              <div class="text-center mb-8">
                <div class="text-5xl mb-4">💰</div>
                <h3 class="text-2xl font-bold mb-2">Estimated Cost Range</h3>
                <p class="text-blue-100">Based on average roofing projects</p>
              </div>
              
              <div class="bg-blue-700/30 backdrop-blur-sm rounded-2xl p-6 mb-8">
                <div class="text-center mb-6">
                  <div class="text-4xl font-bold mb-2">$11,500 - $18,900</div>
                  <p class="text-blue-200">For a 2,500 sq. ft. asphalt shingle roof</p>
                </div>
                
                <div class="space-y-4">
                  <div class="flex justify-between">
                    <span class="text-blue-200">Materials:</span>
                    <span class="font-bold">$4,500 - $7,500</span>
                  </div>
                  <div class="flex justify-between">
                    <span class="text-blue-200">Labor:</span>
                    <span class="font-bold">$5,400 - $9,000</span>
                  </div>
                  <div class="flex justify-between">
                    <span class="text-blue-200">Additional Costs:</span>
                    <span class="font-bold">$1,200 - $2,400</span>
                  </div>
                  <div class="border-t border-blue-500/30 pt-4 mt-4">
                    <div class="flex justify-between text-lg">
                      <span>Total Estimate:</span>
                      <span class="font-bold text-xl">$11,500 - $18,900</span>
                    </div>
                  </div>
                </div>
              </div>
              
              <div class="text-center">
                <p class="text-blue-200 text-sm mb-4">
                  💡 <span class="font-medium">Get quotes from 3-5 local contractors</span> for the most accurate pricing
                </p>
                <a href="#find-contractors" class="inline-block px-6 py-3 bg-white text-blue-600 font-bold rounded-xl hover:bg-blue-50 transition-colors">
                  👷 FIND LOCAL CONTRACTORS
                </a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ====================== -->
  <!-- AD SLOT 3: IN-CONTENT NATIVE AD -->
  <!-- ====================== -->
  <div class="container mx-auto px-4 py-8">
    <div class="bg-white border-2 border-dashed border-gray-300 rounded-2xl p-6">
      <div class="flex items-center justify-between mb-4">
        <div class="text-gray-600 font-medium">RECOMMENDED FOR YOU</div>
        <div class="text-xs text-gray-500 bg-gray-100 px-2 py-1 rounded">Native Ad</div>
      </div>
      <div class="bg-gray-50 rounded-xl p-4">
        <div class="flex items-center gap-4">
          <div class="w-16 h-16 bg-gradient-to-r from-blue-400 to-green-400 rounded-lg flex items-center justify-center">
            <span class="text-white text-2xl">🔧</span>
          </div>
          <div class="flex-1">
            <div class="text-gray-800 font-medium">Roofing Tools & Equipment</div>
            <div class="text-gray-500 text-sm mt-1">Find the best tools for your roofing project</div>
          </div>
        </div>
        <div class="mt-4 text-center">
          <div class="h-[90px] bg-gradient-to-r from-blue-50 to-gray-100 rounded-lg flex items-center justify-center">
            <span class="text-gray-400">Native ad content area</span>
          </div>
        </div>
      </div>
      <div class="mt-4 text-xs text-gray-500 text-center">
        Blends with content for better engagement
      </div>
    </div>
  </div>

  <!-- 👷 CONTRACTOR FINDER -->
  <section id="find-contractors" class="py-20 bg-white">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-6">
          <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-600 to-amber-500">
            Find Trusted Local Roofers
          </span>
        </h2>
        <p class="text-xl text-gray-600 max-w-2xl mx-auto">
          Verified contractors with reviews, ratings, and free estimates
        </p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-6xl mx-auto mb-12">
        {/* Search Input */}
        <div class="md:col-span-2">
          <div class="relative">
            <div class="absolute left-4 top-1/2 transform -translate-y-1/2 text-gray-400">
              📍
            </div>
            <input type="text" 
                   placeholder="Enter your city or zip code..." 
                   class="w-full pl-12 pr-4 py-4 bg-gray-50 border border-gray-200 rounded-xl text-lg focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-200">
          </div>
        </div>
        
        <div>
          <button class="w-full py-4 bg-gradient-to-r from-blue-600 to-blue-700 text-white font-bold rounded-xl hover:shadow-xl hover:shadow-blue-500/30 transition-all">
            🔍 SEARCH CONTRACTORS
          </button>
        </div>
      </div>
      
      {/* Featured Contractors */}
      <div class="grid grid-cols-1 lg:grid-cols-3 gap-8 max-w-6xl mx-auto">
        {[1, 2, 3].map(i => (
          <div class="bg-white border border-gray-200 rounded-2xl overflow-hidden hover:border-blue-400 hover:shadow-xl transition-all">
            <div class="p-6">
              <div class="flex items-start justify-between mb-4">
                <div>
                  <h3 class="text-xl font-bold text-gray-900 mb-1">Premium Roofing Co.</h3>
                  <div class="flex items-center gap-2">
                    <div class="flex text-amber-500">
                      {"★".repeat(5)}
                    </div>
                    <span class="text-gray-600 text-sm">(4.8/5 • 124 reviews)</span>
                  </div>
                </div>
                <div class="px-3 py-1 bg-green-100 text-green-700 text-sm font-bold rounded-full">
                  ⭐ TOP RATED
                </div>
              </div>
              
              <div class="space-y-3 mb-6">
                <div class="flex items-center gap-3">
                  <div class="text-gray-400">📍</div>
                  <span class="text-gray-700">Serving: 50-mile radius</span>
                </div>
                <div class="flex items-center gap-3">
                  <div class="text-gray-400">📅</div>
                  <span class="text-gray-700">25+ years experience</span>
                </div>
                <div class="flex items-center gap-3">
                  <div class="text-gray-400">🏆</div>
                  <span class="text-gray-700">GAF Master Elite Contractor</span>
                </div>
              </div>
              
              <div class="flex items-center justify-between">
                <div>
                  <div class="text-sm text-gray-600">Free estimates:</div>
                  <div class="text-blue-600 font-bold">YES</div>
                </div>
                <div>
                  <div class="text-sm text-gray-600">Warranty:</div>
                  <div class="text-green-600 font-bold">25 years</div>
                </div>
                <div>
                  <div class="text-sm text-gray-600">Insurance:</div>
                  <div class="text-green-600 font-bold">Fully insured</div>
                </div>
              </div>
            </div>
            
            <div class="bg-gray-50 px-6 py-4 border-t border-gray-200">
              <button class="w-full py-3 bg-blue-600 text-white font-bold rounded-lg hover:bg-blue-700 transition-colors">
                📞 REQUEST FREE QUOTE
              </button>
            </div>
          </div>
        ))}
      </div>
      
      <div class="text-center mt-12">
        <a href="/local-contractors" 
           class="inline-flex items-center gap-2 text-blue-600 font-bold text-lg hover:text-blue-700 transition-colors">
          👷 VIEW ALL VERIFIED CONTRACTORS
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" />
          </svg>
        </a>
      </div>
    </div>
  </section>

  <!-- ====================== -->
  <!-- AD SLOT 4: SIDEBAR-STYLE -->
  <!-- ====================== -->
  <div class="container mx-auto px-4 py-8">
    <div class="max-w-6xl mx-auto">
      <div class="grid grid-cols-1 lg:grid-cols-4 gap-8">
        <div class="lg:col-span-3">
          <!-- Content would go here -->
        </div>
        <div class="lg:col-span-1">
          <div class="bg-gradient-to-b from-blue-50 to-white border border-blue-200 rounded-xl p-5">
            <div class="text-blue-700 font-bold text-sm mb-3">SPONSORED</div>
            <div class="bg-white rounded-lg p-4 shadow-sm">
              <div class="h-[600px] flex flex-col items-center justify-center">
                <div class="text-3xl mb-4">🏢</div>
                <div class="text-gray-700 font-medium text-center mb-2">Roofing Companies</div>
                <div class="text-gray-500 text-sm text-center mb-6">National & local roofing services</div>
                <div class="text-gray-400 text-center">160x600 Wide Skyscraper</div>
                <div class="mt-8 text-xs text-gray-500 text-center">
                  Perfect for contractor directories<br/>and roofing service ads
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- 📚 LATEST GUIDES -->
  <section class="py-20 bg-gradient-to-b from-gray-50 to-white">
    <div class="container mx-auto px-4">
      <div class="flex flex-col lg:flex-row justify-between items-start lg:items-center mb-12 gap-6">
        <div>
          <h2 class="text-3xl lg:text-4xl font-bold text-gray-900 mb-2">
            Latest Roofing Guides & Tips
          </h2>
          <p class="text-gray-600">
            Fresh insights to help you make smart roofing decisions
          </p>
        </div>
        <a href="/blog" 
           class="px-6 py-3 bg-gray-200 hover:bg-gray-300 text-gray-900 font-semibold rounded-xl transition-colors flex items-center gap-2">
          <span>📚</span> View All Guides
        </a>
      </div>
      
      {latestPosts && latestPosts.length > 0 ? (
        <>
          <Posts posts={latestPosts} className="mb-12" />
          <div class="text-center">
            <Pagination currentPage={1} totalPages={Math.ceil(allPosts.length / 6)} />
          </div>
        </>
      ) : (
        <div class="text-center py-16">
          <div class="text-5xl mb-6 text-gray-300">📖</div>
          <h3 class="text-2xl font-bold text-gray-500 mb-4">No posts yet</h3>
          <p class="text-gray-400">Our first roofing guides will be published soon!</p>
        </div>
      )}
    </div>
  </section>

  <!-- ====================== -->
  <!-- AD SLOT 5: BETWEEN CONTENT SECTIONS -->
  <!-- ====================== -->
  <div class="container mx-auto px-4 py-8">
    <div class="bg-gradient-to-r from-amber-50 to-blue-50 border-2 border-dashed border-amber-300 rounded-2xl p-6 text-center">
      <div class="text-amber-700 font-bold mb-3">ADVERTISEMENT OPPORTUNITY</div>
      <div class="text-gray-600 mb-4">Positioned between high-engagement content sections</div>
      <div class="bg-white/90 rounded-xl p-4">
        <div class="h-[120px] flex items-center justify-center">
          <div class="text-center">
            <div class="text-gray-500 font-medium">468x60 Banner Ad</div>
            <div class="text-gray-400 text-sm mt-2">Perfect for insurance companies & financing</div>
          </div>
        </div>
      </div>
      <div class="mt-4 text-xs text-gray-500">
        Homeowners viewing this section are actively researching roofing
      </div>
    </div>
  </div>

  <!-- 🎯 QUICK START GUIDES -->
  <section class="py-20 bg-white">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-6">
          <span class="text-transparent bg-clip-text bg-gradient-to-r from-green-500 to-blue-500">
            Start Here → Get Answers
          </span>
        </h2>
        <p class="text-xl text-gray-600 max-w-2xl mx-auto">
          Essential guides to answer your most urgent roofing questions
        </p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 max-w-6xl mx-auto">
        {quickGuides && quickGuides.length > 0 ? quickGuides.map((post, index) => (
          <a href={`/blog/${post.slug}`} 
             class="group bg-white border border-gray-200 rounded-2xl overflow-hidden hover:border-blue-500 hover:shadow-lg transition-all duration-300 hover:-translate-y-1 flex flex-col h-full">
            
            {/* Thumbnail */}
            <div class="relative h-48 overflow-hidden bg-gradient-to-br from-blue-500/10 to-green-500/10">
              {post.data.image ? (
                <img
                  src={post.data.image}
                  alt={post.data.title}
                  class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
                  loading="lazy"
                />
              ) : (
                <div class="absolute inset-0 flex items-center justify-center">
                  <div class="text-5xl opacity-20">
                    {post.data.category === 'roof-cost-estimates' ? '💰' : 
                     post.data.category === 'material-comparisons' ? '🏠' :
                     post.data.category === 'contractor-guides' ? '👷' :
                     '📚'}
                  </div>
                </div>
              )}
              <div class="absolute top-3 left-3">
                <span class="inline-block px-3 py-1 bg-gradient-to-r from-green-500 to-blue-500 text-white text-xs font-bold rounded-full shadow-md">
                  {post.data.readTime <= 5 ? '⚡ QUICK READ' : '📖 ESSENTIAL GUIDE'}
                </span>
              </div>
            </div>
            
            {/* Content */}
            <div class="p-5 flex-1 flex flex-col">
              <div class="mb-3">
                <div class="text-xs font-semibold text-blue-600 uppercase tracking-wider mb-1">
                  {post.data.category ? post.data.category.replace('-', ' ') : 'Guide'}
                </div>
                <h3 class="text-lg font-bold text-gray-900 mb-3 group-hover:text-blue-600 transition-colors line-clamp-2">
                  {post.data.title}
                </h3>
              </div>
              
              <p class="text-gray-600 text-sm mb-4 flex-1 line-clamp-3">
                {post.data.description || 'Essential roofing information...'}
              </p>
              
              <div class="flex items-center justify-between mt-auto pt-4 border-t border-gray-100">
                <span class="text-blue-600 text-sm font-medium flex items-center gap-1">
                  READ GUIDE
                  <svg class="w-4 h-4 transform group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" />
                  </svg>
                </span>
                <span class="text-gray-400 text-xs bg-gray-100 px-2 py-1 rounded">
                  {post.data.readTime} min
                </span>
              </div>
            </div>
          </a>
        )) : (
          <div class="col-span-4 text-center py-12">
            <div class="text-4xl mb-4 text-gray-300">📚</div>
            <h3 class="text-xl font-bold text-gray-500">No guides yet</h3>
            <p class="text-gray-400 mt-2">Check back soon for our first roofing guides!</p>
          </div>
        )}
      </div>
    </div>
  </section>

  <!-- 📧 NEWSLETTER SIGNUP -->
  <section class="py-20 bg-gradient-to-r from-blue-50 via-white to-amber-50">
    <div class="container mx-auto px-4">
      <div class="max-w-3xl mx-auto text-center">
        <div class="bg-white/80 backdrop-blur-sm rounded-3xl border border-blue-200 p-8 lg:p-10 shadow-xl">
          <div class="text-5xl mb-6">📧</div>
          
          <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-6">
            Get <span class="text-blue-600">Free Roofing Reports</span> & Exclusive Tips
          </h2>
          
          <p class="text-gray-700 text-lg mb-8">
            Join thousands of homeowners who get exclusive roofing guides, cost calculators, and contractor discounts.
          </p>
          
          {/* Beehiiv Embed */}
          <div class="mb-8">
            <script async src="https://subscribe-forms.beehiiv.com/embed.js"></script>
            <iframe 
              src="https://subscribe-forms.beehiiv.com/967a3a10-7629-4938-af89-881c58918e73" 
              class="beehiiv-embed" 
              data-test-id="beehiiv-embed" 
              frameborder="0" 
              scrolling="no" 
              style="width: 560px; height: 314px; margin: 0 auto; border-radius: 8px; background-color: transparent; box-shadow: 0 0 #0000; max-width: 100%;">
            </iframe>
          </div>
          
          <div class="mt-8 pt-6 border-t border-gray-200">
            <p class="text-gray-500 text-xs">
              ⚠️ <span class="text-gray-600">A Txchyon Capital property. By subscribing, you agree to our <a href="/privacy" class="text-blue-600 hover:underline">Privacy Policy</a>.
              No spam. Unsubscribe anytime.</span>
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ====================== -->
  <!-- AD SLOT 6: FOOTER (EXIT INTENT) -->
  <!-- ====================== -->
  <div class="container mx-auto px-4 py-8">
    <div class="bg-gradient-to-r from-gray-100 to-gray-200 border-2 border-gray-300 rounded-2xl p-6 text-center">
      <div class="text-gray-700 font-bold mb-3">BEFORE YOU GO...</div>
      <div class="text-gray-600 text-sm mb-4">Exit-intent ad placement - Last chance to reach engaged visitors</div>
      <div class="bg-white rounded-xl p-4">
        <div class="h-[250px] flex flex-col items-center justify-center">
          <div class="text-3xl mb-3">🚀</div>
          <div class="text-gray-700 font-medium">Roofing Business Services</div>
          <div class="text-gray-500 text-sm mt-2">Software, insurance, financing solutions</div>
          <div class="mt-6 text-gray-400">300x250 Rectangle</div>
        </div>
      </div>
      <div class="mt-4 text-xs text-gray-500">
        Visitors about to leave - high impact for roofing service providers
      </div>
    </div>
  </div>

  <!-- 🎯 FINAL CTA -->
  <section class="py-20 bg-gradient-to-r from-blue-600 to-blue-800">
    <div class="container mx-auto px-4">
      <div class="max-w-3xl mx-auto text-center text-white">
        <div class="bg-white/10 backdrop-blur-sm rounded-3xl border border-white/20 p-8 lg:p-10">
          <div class="text-5xl mb-6">🎯</div>
          
          <h2 class="text-3xl md:text-4xl font-bold mb-6">
            Ready to Start Your Roofing Project?
          </h2>
          
          <p class="text-blue-100 text-lg mb-8">
            Get accurate estimates, compare materials, and find trusted contractors—all in one place.
          </p>
          
          <div class="space-y-4">
            <a href="#cost-calculator" 
               class="block w-full py-4 bg-white text-blue-600 font-bold rounded-xl text-lg hover:bg-blue-50 hover:shadow-xl transition-all">
              💰 GET FREE COST ESTIMATE
            </a>
            
            <p class="text-blue-200 text-sm">
              ⚡ Join {realisticStats.homeownersHelped} homeowners making smart roofing decisions
            </p>
          </div>
          
          {/* Disclaimer */}
          <div class="mt-8 pt-6 border-t border-white/20">
            <p class="text-blue-200 text-xs">
              ⚠️ <span class="text-white">Disclaimer: Estimates are based on average costs. Actual prices may vary.
              Always get multiple quotes from licensed contractors. RoofingMeter does not receive commissions from contractors.</span>
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Add Custom Styles -->
  <style>
    @keyframes gradient {
      0%, 100% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
    }
    
    .animate-gradient {
      background-size: 200% 200%;
      animation: gradient 4s ease infinite;
    }
    
    /* Line clamp utility */
    .line-clamp-1 {
      overflow: hidden;
      display: -webkit-box;
      -webkit-box-orient: vertical;
      -webkit-line-clamp: 1;
    }
    
    .line-clamp-2 {
      overflow: hidden;
      display: -webkit-box;
      -webkit-box-orient: vertical;
      -webkit-line-clamp: 2;
    }
    
    .line-clamp-3 {
      overflow: hidden;
      display: -webkit-box;
      -webkit-box-orient: vertical;
      -webkit-line-clamp: 3;
    }
    
    /* Smooth hover effects */
    * {
      transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
    }
    
    /* Beehiiv embed styling */
    .beehiiv-embed {
      border-radius: 8px !important;
    }
    
    /* Ad styling */
    .ad-slot {
      transition: all 0.3s ease;
    }
    
    .ad-slot:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1);
    }
  </style>
</Base>